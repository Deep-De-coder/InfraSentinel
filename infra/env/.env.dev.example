# InfraSentinel DEV mode - NetBox + real evidence store
# Copy to .env and use with: docker compose --profile dev up -d

INFRASENTINEL_MODE=dev
CV_MODE=mock
NETBOX_MODE=netbox
EVIDENCE_BACKEND=minio

# API auth (recommended for dev)
INFRA_API_KEY=dev-secret-change-me
AUTH_READS=false

# Temporal
TEMPORAL_ADDRESS=temporal:7233
TEMPORAL_TASK_QUEUE=infrasentinel-task-queue

# Database
DATABASE_URL=postgresql+asyncpg://infrasentinel:infrasentinel@app-postgres:5432/infrasentinel

# MinIO
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=minio123
MINIO_BUCKET=infrasentinel-evidence

# NetBox (create token in UI after first start)
NETBOX_URL=http://netbox:8080
NETBOX_TOKEN=

# A2A
A2A_MODE=off
A2A_MOP_URL=http://a2a-mop-agent:8091
A2A_VISION_URL=http://a2a-vision-agent:8092
A2A_CMDB_URL=http://a2a-cmdb-agent:8093
